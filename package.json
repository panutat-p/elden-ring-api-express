{
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --minify --sourcemap --platform=node --target=es2022 --outfile=dist/index.js",
    "lint": "prettier --write ."
  },
  "devDependencies": {
    "esbuild": "^0.17.12",
    "prettier": "^2.8.6",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  }
}
